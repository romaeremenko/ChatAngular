import { BlockWidthDirective } from './block-width.directive';

describe('BlockWidthDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new BlockWidthDirective();
  //   expect(directive).toBeTruthy();
  // });
});
